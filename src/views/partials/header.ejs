<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <!-- ✅ viewport correto -->
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
  <title>Maquininhas</title>

  <link rel="stylesheet" href="/css/style.css" />

  <style>
    * { box-sizing: border-box; }

    /* ❌ NÃO TRAVA o site no mobile */
    html, body{
      width: 100%;
      max-width: 100%;
      overflow-x: auto;
    }

    body {
      margin: 0;
      font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
      background: #f4f6f9;
    }

    /* =========================
       LAYOUT BASE (DESKTOP)
    ========================== */
    .app-shell{
      min-height: 100vh;
      display: flex;
      width: 100%;
      max-width: 100%;
    }

    /* sidebar (desktop) */
    .sidebar{
      width: 220px;
      flex: 0 0 220px;
      background: #0b3b8f;
      color: #fff;
      min-height: 100vh;
    }

    /* área direita (header + conteúdo) */
    .app-main{
      flex: 1;
      min-width: 0;
      display: flex;
      flex-direction: column;
      max-width: 100%;
    }

    /* header topo */
    .top-header{
      height: 60px;
      background: #fff;
      border-bottom: 1px solid #e5e5e5;
      display: flex;
      align-items: center;
      padding: 0 16px;
      gap: 12px;
      min-width: 0;
    }

    .btn-menu{
      display: none; /* só aparece no modo mobile */
      border: 1px solid #ddd;
      background: #fff;
      border-radius: 10px;
      padding: 8px 10px;
      font-size: 18px;
      line-height: 1;
      cursor: pointer;
    }

    .top-header-right{
      margin-left: auto;
      display: flex;
      align-items: center;
      gap: 14px;
      min-width: 0;
    }

    .user-info{
      font-size: 15px;
      font-weight: 600;
      color: #333;
      white-space: nowrap;
      max-width: 55vw;
      overflow: hidden;
      text-overflow: ellipsis;
    }

    .logout-btn{
      background: #ff4d4d;
      padding: 8px 14px;
      border-radius: 8px;
      color: white !important;
      font-weight: 700;
      text-decoration: none;
      transition: 0.2s;
      white-space: nowrap;
      flex: 0 0 auto;
    }
    .logout-btn:hover{ background: #cc0000; }

    /* conteúdo padrão */
    .main-content{
      padding: 18px;
      width: 100%;
      max-width: 100%;
      min-width: 0;
    }

    /* overlay (mobile) */
    .overlay{
      display: none;
      position: fixed;
      inset: 0;
      background: rgba(0,0,0,0.35);
      z-index: 998;
    }
    .overlay.show{ display: block; }

    /* =========================
       ✅ MOBILE (breakpoint maior)
       - resolve quando o browser renderiza como 980px
    ========================== */
    @media (max-width: 1200px){
      .app-shell{ display: block; }

      .btn-menu{ display: inline-flex; align-items:center; justify-content:center; }

      /* sidebar vira drawer */
      .sidebar{
        position: fixed;
        top: 0;
        left: 0;
        height: 100vh;
        width: 84vw;
        max-width: 320px;
        transform: translateX(-100%);
        transition: transform .2s ease;
        z-index: 999;
        overflow-y: auto;
      }
      .sidebar.open{ transform: translateX(0); }

      .main-content{ padding: 12px; }
    }
  </style>
</head>

<body>

<div class="overlay" id="overlay" onclick="closeMenu()"></div>

<div class="app-shell">
